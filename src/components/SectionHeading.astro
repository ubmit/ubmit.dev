---
interface Props {
  title: string;
  href?: string;
}

const { title, href } = Astro.props;
---

<h2 class="ledger-label">
  {
    Boolean(href) ? (
      <a class="hover:text-gray-1100" href={href}>
        {title}
      </a>
    ) : (
      <span>{title}</span>
    )
  }
</h2>
