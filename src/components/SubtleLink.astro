---
interface Props {
  href: string;
  target?: string;
}

const { href, target } = Astro.props;
---

<a
  href={href}
  target={target}
  rel="noopener noreferrer"
  class="underline decoration-[var(--ledger-line)] underline-offset-4 transition-colors hover:text-gray-1200"
>
  <slot /></a
>
