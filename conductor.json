{
    "scripts": {
        "setup": "ln -s \"$CONDUCTOR_ROOT_PATH/.env\" .env; pnpm install",
        "run": "pnpm run dev --port $CONDUCTOR_PORT",
        "archive": "rm -rf \"$HOME/Library/Application Support/com.conductor.app.dev.$CONDUCTOR_WORKSPACE_NAME\""
    }
}